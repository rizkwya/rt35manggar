import React from 'react';

interface PageTransitionLoaderProps {
  isLoading?: boolean;
}

// Clean non-intrusive component - no annoying loading overlays
export const PageTransitionLoader: React.FC<PageTransitionLoaderProps> = () => {
  return null;
};
